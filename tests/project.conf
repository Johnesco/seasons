# Seasons — project configuration
PROJECT_NAME=seasons
ENGINE=inform7

# Paths
GAME_FILE=seasons.ulx
SOURCE_FILE=story.ni

# Scoring: the demo has no score, so pass on reaching the end of the cycle.
# The falling-leaves description only prints after "examine leaves" in Fall,
# the last stop before the loop returns to Winter.
DEFAULT_MAX_SCORE=0
SCORELESS_GAME=true
WON_PATTERNS='catching the light before it settles'

# Pipeline
PIPELINE_SOUND=false
PIPELINE_HUB_ID=seasons
PIPELINE_TESTS=walkthrough
